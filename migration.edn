{:schema "etzhayyim.migration/v1" :source {:repository "etzhayyim/root" :revision "e5654f08638e1785f519566582bb3c4dc7719f45" :path "60-apps/etzhayyim-project-isin" :tree "46ea1dd7375db898091064bd1992a7050e3d60d8" :tracked-files 26 :bytes 71590} :destination {:repository "etzhayyim/com-etzhayyim-app-isin"} :identity {:allowed-additions ["README.edn" "migration.edn"]} :deviations [{:date "2026-08-26" :change "cljs-migration" :description "Frontend migrated from SvelteKit (appview/etzhayyim-wasm-isin-is1n8k2x/svelte/) to ClojureScript (shadow-cljs + reagent + re-frame + jp-go-dds), added at appview/etzhayyim-wasm-isin-is1n8k2x/cljs/. appview/etzhayyim-wasm-isin-is1n8k2x/svelte/ was deleted (git rm), so :tracked-files/:bytes above no longer describe the working tree as it stands after this commit -- they remain the honest record of what the original import actually copied. The one non-frontend file svelte/ held, the SvelteKit XRPC server route (src/routes/xrpc/[...path]/+server.ts), was moved byte-for-byte (only a header comment prepended) to appview/etzhayyim-wasm-isin-is1n8k2x/src/xrpc-mcp-router-proxy.ts, not deleted, since it is backend code out of scope for a frontend migration. The other static asset svelte/ held, static/health.json (served at /health.json), was likewise moved verbatim to appview/etzhayyim-wasm-isin-is1n8k2x/cljs/public/health.json so that URL keeps resolving."}]}
